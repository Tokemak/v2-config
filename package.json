{
  "name": "v2-config",
  "version": "1.0.0",
  "private": true,
  "description": "Tokemak v2 autopilot configuration — JSON data + self-served HTTP API",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.13"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
